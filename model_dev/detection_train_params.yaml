path: ./model_dev/data_detection # dataset root dir - absolute path
train: images/train # train images (relative to 'path')
val: images/test # val images (relative to 'path')

# Classes
names:
  0: symbol
